import 'dart:ui';

class CustomColors {
static const Color startButton = Color.fromRGBO(117, 25, 235, 1);
static  Color shadowStartButton = const Color.fromARGB(255, 107, 107, 107).withOpacity(0.5);

}